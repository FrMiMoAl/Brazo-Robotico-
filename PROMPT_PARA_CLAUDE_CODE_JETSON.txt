Actúa como un ingeniero senior de ROS 2, percepción 3D y robótica segura en Jetson.

Estás trabajando en la Jetson del proyecto de Franco. El objetivo es implementar la arquitectura descrita en el archivo:

/home/franco/ros2_ws/LLM_KINECT_BRAZO_JETSON_SPEC.md

Primero lee el archivo completo. Después inspecciona el workspace actual en:

/home/franco/ros2_ws

Reglas obligatorias:
1. No borres el workspace.
2. No borres src/.
3. No cambies el controlador real del brazo si ya funciona; inserta la nueva arquitectura por encima.
4. Crea una rama Git feature/llm-kinect-brazo si el repo usa Git. Si no usa Git, crea un backup de src/ antes de modificar.
5. Verifica ROS_DISTRO con echo $ROS_DISTRO y usa /opt/ros/$ROS_DISTRO/setup.bash.
6. Implementa por fases, compilando después de cada fase importante.
7. El LLM nunca debe publicar directamente en /joint_commands, /target_position ni /gripper_command.
8. yolo_depth_to_point.py no debe publicar /target_position por defecto; solo puede hacerlo con publish_direct_target:=true para debug.
9. Toda percepción 3D debe usar PointStamped u objetos con Header/frame_id.
10. La transformación cámara → base debe hacerse con TF2, no con offsets como ruta principal.
11. El sistema debe arrancar en dry_run:=true, autonomous_enable:=false y hardware_armed:=false.
12. safety_guard_node debe ser la única puerta hacia /target_position y /gripper_command.
13. No inventes valores reales de calibración TF. Deja placeholders claros X Y Z ROLL PITCH YAW y explica cómo medirlos.
14. Prioriza un MVP robusto: detectar objeto rojo por HSV, publicar scene_state, generar plan JSON, ejecutar pick-and-place en dry_run.

Tareas concretas:
- Crear paquete brazo_interfaces con los mensajes y acciones definidos en el MD.
- Refactorizar object_3d_detector/yolo_depth_to_point.py para publicar /perception/objects_camera y /perception/selected_object_camera.
- Crear paquete brazo_ai con:
  - camera_to_base_node.py
  - scene_state_node.py
  - llm_agent_node.py
  - manual_plan_node.py
  - task_executor_node.py
  - safety_guard_node.py
  - launch/llm_kinect_brazo.launch.py
- Mantener compatibilidad con el brazo existente que escucha /target_position y /gripper_command.
- Agregar logs claros y parámetros de seguridad.
- Crear o actualizar README con comandos de prueba.
- Ejecutar colcon build y corregir errores.
- Al final, mostrar:
  - archivos creados/modificados,
  - comandos de lanzamiento,
  - comandos de prueba,
  - qué queda pendiente para calibrar la Kinect con base_link.

Criterio de terminado:
- colcon build funciona.
- /perception/objects_camera existe.
- /perception/objects_base existe.
- /scene_state publica JSON.
- /llm_plan recibe un plan al publicar /user_command.
- task_executor_node ejecuta la secuencia en dry_run.
- safety_guard_node bloquea comandos reales si hardware_armed=false.

Empieza ahora inspeccionando el workspace y luego implementa FASE 0 a FASE 12 del MD.
